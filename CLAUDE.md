# CLAUDE.md

The agent instructions for this repository live in [AGENTS.md](AGENTS.md) so they are
shared across every tool (Codex, Claude, OpenCode). Claude Code does not auto-load
`AGENTS.md`, so it is imported below — keep the actual rules in `AGENTS.md`, not here.

@AGENTS.md
