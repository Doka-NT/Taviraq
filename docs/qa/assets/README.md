# QA Screenshot Assets

Store optional reference screenshots here when a test case marks `Screenshot: TODO` or references an asset path.

Guidelines:

- Use screenshots only when visual state is part of acceptance criteria.
- Prefer focused screenshots of the relevant UI state.
- Do not include screenshots containing real API keys, hostnames, usernames, private repository paths, or terminal secrets.
- Keep filenames stable when test cases reference them.

